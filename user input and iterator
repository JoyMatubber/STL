#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n,a;
    cin>>n;
    vector<int> v;
   vector<int>::iterator i;
   for(int i=0;i<n;i++)
   {
       cin>>a;
       v.push_back(a);
   }
   for(i=v.begin();i!=v.end();i++)
   {
       cout<<*i<<" ";
   }

}
