#include <bits/stdc++.h>

using namespace std;

int main()
{

int i,n,a;
cin>>n;



    vector<int> v;
    vector<int> :: iterator it;
    for(i=0;i<n;i++)
    {
        cin>>a;
        v.push_back(a);
    }
 it = max_element(v.begin(),v.end());
   cout<<*it<<endl;
}
