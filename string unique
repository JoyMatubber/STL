#include <bits/stdc++.h>
using namespace std;
//#define optimize() ios_base:: sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main()
{
int i ,n;
cin>>n;
string a;

vector<string> s;
for(i=0;i<n;i++)
{
    cin>>a;
   s.push_back(a);


}
sort(s.begin(),s.end());
int sa=unique(s.begin(),s.end())-s.begin();
for(i=0;i<sa;i++)
{
   cout<<s[i]<<endl;

}










}
